{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@plugnrent/common,@plugnrent/server}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
